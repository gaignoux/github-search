/**
 * Represents an empty state with text and an optional button.
 */
export type TEmptyState = {
  /**
   * The text to display in the empty state.
   */
  text: string;
  /**
   * Whether to show a button in the empty state.
   */
  showButton?: boolean;
};
