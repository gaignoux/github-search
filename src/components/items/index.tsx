import { ReactElement } from "react";

type Props = {
  list: {
    name: string;
    user: string;
    description: string;
  }[];
};
export const Items = (props: Props): ReactElement => {
  return <div></div>;
};
